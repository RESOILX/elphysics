[pytest]
# テストの探索場所
testpaths = tests

# import パス:
#   .  → プロジェクトルートを追加し `import elphysics` を解決
pythonpath = .

# 出力を簡潔に。テスト名と進捗を表示。
addopts = -v --tb=short

# テスト関数/クラスの命名規約 (デフォルト通り)
python_files = test_*.py
python_classes = Test*
python_functions = test_*
